#if($ctx.error)
    $utils.error($ctx.error.message, $ctx.error.type)
#end

{
    "chats": $utils.toJson($context.result.items)
    #if( ${context.result.nextToken} )
        ,"nextToken": $util.toJson($context.result.nextToken)
    #else
        ,"nextToken": ""
    #end
}